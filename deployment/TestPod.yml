apiVersion: v1
kind: Pod
metadata:
  name: zjgpu-test
spec:
  restartPolicy: OnFailure
  containers:
  - image: alpine
    name: zjgpu-test-ctr
    resources:
      limits:
        github.com/zjGPU: 10
